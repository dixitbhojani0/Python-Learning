[pytest]
asyncio_mode = auto
markers =
    integration: tests that require Qdrant + Redis Docker containers
    smoke: tests that require the full FastAPI server running on localhost:8000
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
