"""
backend/api/routes/admin.py

DEPRECATED — this file is kept as a redirect only because it is open in an editor.
All content has been moved to the admin/ package:
  admin/models.py   — Pydantic request/response models
  admin/helpers.py  — stat helper functions
  admin/router.py   — all 15 endpoint handlers

main.py imports `from backend.api.routes import admin` which will pick up
the admin/ package (folder beats file in Python's import resolution).
This file is no longer imported by anything.
"""
