📄 Documents (sprint docs, ADRs, Slack, incidents, version policies)
        │
        ▼
┌─────────────────┐
│  DocumentChunker │
│                 │
│  text → Parent  │  ~1500 tokens  (paragraph-aware split)
│         │       │
│         └─Child │  ~350 tokens   (sliding window, 40-token overlap)
└─────────────────┘
        │
        ▼
┌──────────────────────────┐
│  Contextual Prefix (LLM) │  "This chunk is from nginx ADR, section 3..."
│  prepend 1-2 sentences   │  + child text  →  richer embedding
└──────────────────────────┘
        │
        ▼
┌──────────────────┐
│  Embedding Model │  all-MiniLM-L6-v2  →  384-dim vector
│  (local, free)   │
└──────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│              Qdrant                     │
│  { vector, parent_text, source,         │
│    doc_type, project, stale: false }    │
└─────────────────────────────────────────┘
